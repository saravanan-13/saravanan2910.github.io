jQuery(function($) {
    "use strict";


	/*==========  INITIAT WOW JS  ==========*/
	new WOW().init();


	/*==========  SIMPLE SLIDER  ==========*/
	if ($('.simple-slider').length > 0) {
		$(".simple-slider").owlCarousel({
			nav: true,
			navText: [
			"<i class='icon-angle-left'></i>",
			"<i class='icon-angle-right'></i>"
			],
			loop: true,
			autoplay: true,
			animateIn: 'fadeIn',
			animateOut: 'fadeOut',
			items: 1
		});
	};

	//for advanced-slider
	if ($('.advanced-slider').length > 0) {
		$( ".advanced-slider .owl-controls" ).addClass( "container");
	};


	/*==========  TEAM SLIDER  ==========*/
	if ($('.team-slider').length > 0) {
		$(".team-slider").owlCarousel({
			dotsContainer: '.team-slider-thumb',
			nav: false,
			loop: true,
			items: 1,
			animateIn: 'fadeIn',
			animateOut: 'fadeOut',
		});
	};

	//Team Slider v2
	if ($('.team-slider-v2').length > 0) {
		$(".team-slider-v2").owlCarousel({
			margin: 35,
			loop: true,
      		nav: true,
			navText: [
			"<i class='icon-angle-left'></i>",
			"<i class='icon-angle-right'></i>"
			],
			responsive:{
				0:{
					items: 1
				},
				480:{
					items:2
				},
				992:{
					items:3
				},
				1366:{
					items:4
				}
			}
		});
	};




	/*==========  PARTNERS SLIDER  ==========*/
	if ($('.partners-slider').length > 0) {
		$(".partners-slider").owlCarousel({
			margin: 30,
			navText: [
			"<i class='icon-angle-left'></i>",
			"<i class='icon-angle-right'></i>"
			],
			loop: true,
			responsive:{
				0:{
					items:2
				},
				768:{
					items:3
				},
				1000:{
					items:6
				}
			}
		});
	};



	/* ======= SHUFFLE JS (FILTERABLE PORTFOLIO) ======= */
	$(window).on('load', function () {

		$('.portfolio-container').each(function(i, e){

			var ttGrid = $(this).find('.portfolio-items');
			var self = this;
			ttGrid.shuffle({
                    itemSelector: '.item' // the selector for the items in the grid
                });

			/* reshuffle when user clicks button filter item */
			$(this).find('.item-filter li').on('click',function (e) {
				e.preventDefault();

                    // set active class
                    $(self).find('.item-filter li').removeClass('active');
                    $(this).addClass('active');

                    // get group name from clicked item
                    var ttGroupName = $(this).attr('data-group');

                    // reshuffle grid
                    ttGrid.shuffle('shuffle', ttGroupName);
                });

		});

	});
	
	/*==========  NAVIGATION  ==========*/
        function c(c)
        {
                var d = a.scrollTop();
                b > d ? ($("#name1, #name2, #name3").removeClass("goUp"), $(".button", "#left_menu").removeClass("goUp")) : b < d && ($("#name1, #name2, #name3").addClass("goUp"), $(".button", "#left_menu").addClass("goUp")), b = d
        }

        function j()
        {
                f.toggleClass("social_no_delay")
        }
        var a = $(window),
                b = 0;
        a.on("scroll", c);
        var d = $(window).height(),
                e = $(window).height() - 94,
                f = $("#footer_soc_container"),
                g = $("#name1"),
                h = $("#name2"),
                i = $("#left_menu");
        $(window).scrollTop() > d && (f.hasClass("footer_soc_icons_show") || i.hasClass("leftm_active") || (f.addClass("footer_soc_icons_show"), setTimeout(j, 1500))), $(window).scrollTop() > e && h.fadeIn(50), $(window).scroll(function()
        {
                function a()
                {
                        f.toggleClass("social_no_delay")
                }

                function a()
                {
                        f.toggleClass("social_no_delay")
                }
                $(window).scrollTop() > d && g.fadeOut(50), $(window).scrollTop() > e && h.fadeIn(50), $(window).scrollTop() < e && h.fadeOut(50), $(window).scrollTop() < d && g.fadeIn(50), $(window).scrollTop() > d ? f.hasClass("footer_soc_icons_show") || i.hasClass("leftm_active") || (f.addClass("footer_soc_icons_show"), setTimeout(a, 1500)) : $(window).scrollTop() < d && f.hasClass("footer_soc_icons_show") && !i.hasClass("leftm_active") && (f.removeClass("footer_soc_icons_show"), setTimeout(a, 1500))
        }), $(this).on("click", ".button_bot", function()
        {
                $("html,body").animate(
                {
                        scrollTop: d
                }, 500)
        }), skrollr.init(
        {
                mobileCheck: function()
                {
                        return !1
                }
        }), $("#left_menu > .button").on("click", function()
        {
                function b()
                {
                        $(".social_icon", "#left_menu").toggleClass("social_no_delay")
                }
                var a = $("#main_workflow");
                $(this).parent().hasClass("leftm_active") ? ($(this).parent().addClass("stop_animation"), $(".social_icon", "#left_menu").toggleClass("social_no_delay")) : ($(this).parent().removeClass("stop_animation"), setTimeout(b, 1500)), $(this).toggleClass("b_active"), $(this).parent().toggleClass("leftm_active"), a.toggleClass("mw_active"), a.toggleClass("pushReady"), a.toggleClass("pushLeft"), $(".name").toggleClass("n_active"), $("#menu_active_overlay").toggleClass("leftm_bg_active")
        }), $("#left_menu .button_menu").on("click", function(a)
        {
                function d()
                {
                        $(".social_icon", "#left_menu").toggleClass("social_no_delay")
                }
                var b = $("#main_workflow");
                a.preventDefault();
                var c = $(this);
                $(this).parents("#left_menu").hasClass("leftm_active") ? ($(this).parents("#left_menu").addClass("stop_animation"), $(".social_icon", "#left_menu").toggleClass("social_no_delay")) : ($(this).parents("#left_menu").removeClass("stop_animation"), setTimeout(d, 1500)), $(this).parents("#left_menu").find(".button").toggleClass("b_active"), $(this).parents("#left_menu").toggleClass("leftm_active"), b.toggleClass("mw_active"), b.toggleClass("pushReady"), b.toggleClass("pushLeft"), $(".name").toggleClass("n_active"), $("#menu_active_overlay").toggleClass("leftm_bg_active"), setTimeout(function()
                {
                        var a = c.attr("href"),
                                b = window.pageYOffset;
                        console.log(b), console.log($(a).offset().top);
                        var d = $(a).offset().top;
                        return jQuery("html:not(:animated),body:not(:animated)").animate(
                        {
                                scrollTop: d
                        }, 1e3), !1
                }, 700)
        }), 
		
		
		
		$("#menu_active_overlay").on("click", function()
        {
                var a = $("#main_workflow");
                $("#left_menu").hasClass("leftm_active") ? $("#left_menu").addClass("stop_animation") : $("#left_menu").removeClass("stop_animation"), $(".button", "#left_menu").toggleClass("b_active"), $("#left_menu").toggleClass("leftm_active"), a.toggleClass("mw_active"), a.toggleClass("pushReady"), a.toggleClass("pushLeft"), $(".name").toggleClass("n_active"), $("#menu_active_overlay").toggleClass("leftm_bg_active"), $(".social_icon", "#left_menu").toggleClass("social_no_delay")
        })
		;
        var k = function(a, b, c)
                {
                        if ("[object Object]" === Object.prototype.toString.call(a))
                                for (var d in a) Object.prototype.hasOwnProperty.call(a, d) && b.call(c, a[d], d, a);
                        else
                                for (var e = 0, f = a.length; e < f; e++) b.call(c, a[e], e, a)
                },
                l = function(a)
                {
                        var b = 0;
                        if (a.offsetParent)
                                do b += a.offsetTop, a = a.offsetParent; while (a);
                        return b >= 0 ? b : 0
                },
                m = document.querySelectorAll(".reveal"),
                n = 100;
        k(m, function(a)
        {
                if (l(a) > window.innerHeight) var b = l(a) - window.innerHeight + n;
                else var b = -1;
                var c = new Headroom(a,
                {
                        offset: b,
                        tolerance: 205,
                        classes:
                        {
                                initial: "revealer",
                                pinned: "revealer--pinned",
                                unpinned: "revealer--unpinned",
                                top: "revealer--hide",
                                notTop: "revealer--show",
                                bottom: "revealer--bottom",
                                notBottom: "revealer--not-bottom"
                        }
                });
                c.init()
        }), $(this).on("submit", ".form_ajax", function(a)
        {})
}), 

	/*==========  LOAD  ==========*/
$(window).on("load", function()
{
        var a = $("#page-preloader"),
                b = a.find(".wraper");
        b.fadeOut(), a.fadeOut(), setTimeout(function()
        {
                centerInit(), $(window).resize(centerInit), $(".border_text", "#first_slide").addClass("border_text_show"), $(".text_block", "#first_slide").addClass("text_block_show"), $(".fsi_helper", "#first_slide").addClass("fsi_show"), $(".name").addClass("name_show"), $("#left_menu > .button").addClass("button_show"), $(".button_bot", "#first_slide").addClass("button_bot_show")
        }, 100), setTimeout(function()
        {
                $(".name").addClass("nodelay")
        }, 800), $(".prj_img", "#my_projects").each(function(a)
        {
                var c, d = ($(this), "url(" + $(this).attr("src") + ")"),
                        e = $(this).parent();
                return e.css("background-image", d), c = new Image, c.src = $(this).attr("src"), $(this).attr("data-width", c.naturalWidth), $(this).attr("data-height", c.naturalHeight)
        })


	/*==========  MAGNIFIC POPUP ==========*/
    if ($('.popup').length > 0) {
        $('.popup').magnificPopup({
            type: 'image',
            mainClass: 'mfp-fade',
            removalDelay: 160,
            fixedContentPos: false,
            gallery: {
                enabled: true,
                navigateByImgClick: true,
                preload: [0, 1]
            }

        });
    };

    // for popup video
    if ($('.popup-video').length > 0) {
    	$('.popup-video').magnificPopup({
    		disableOn: 700,
    		type: 'iframe',
    		mainClass: 'mfp-fade',
    		removalDelay: 160,
    		preloader: false,
    		fixedContentPos: false
    	});
    };


	/*==========  COUNTER  ==========*/
	$('.counter, .counter-v2, .social-feedback').bind('inview', function(event, visible, visiblePartX, visiblePartY) {
		if (visible) {
			$(this).find('.timer').each(function () {
				var $this = $(this);
				$({ Counter: 0 }).animate({ Counter: $this.text() }, {
					duration: 2000,
					easing: 'swing',
					step: function () {
						$this.text(Math.ceil(this.Counter));
					}
				});
			});
			$(this).unbind('inview');
		}
	});


	/*==========  TOOLTIP  ==========*/
	$(function () {
		$('[data-toggle="tooltip"]').tooltip()
	});


	/*==========  ACCORDIAN  ==========*/
	if ($('.accordion-toggle').length > 0) {
		$('.accordion-toggle').on('click', function(){
			$(this).closest('.panel-group').children().each(function(){
				$(this).find('>.panel-heading').removeClass('active');
			});

			$(this).closest('.panel-heading').toggleClass('active');
		});
	};





	/*==========  GOTO TOP  ==========*/
	$(window).scroll(function() {
		if ($(this).scrollTop() > 500) {
			$('.go-top').addClass('active');
		} else {
			$('.go-top').removeClass('active');
		}
	});
	// Animate the scroll to top
	$('.go-top').click(function(event) {
		event.preventDefault();

		$('html, body').animate({scrollTop: 0}, 500);
	})



	/*==========  BUTTON RIPPLE EFFECT  ==========*/
  	if ($('.ripple').length > 0) {
  		$('.ripple').on('click', function (event) {
  			event.preventDefault();

  			var $div = $('<div/>'),
  			btnOffset = $(this).offset(),
  			xPos = event.pageX - btnOffset.left,
  			yPos = event.pageY - btnOffset.top;



  			$div.addClass('ripple-effect');
  			var $ripple = $(".ripple-effect");

  			$ripple.css("height", $(this).height());
  			$ripple.css("width", $(this).height());
  			$div
  			.css({
  				top: yPos - ($ripple.height()/2),
  				left: xPos - ($ripple.width()/2),
  				background: $(this).data("ripple-color")
  			})
  			.appendTo($(this));

  			window.setTimeout(function(){
  				$div.remove();
  			}, 2000);
  		});
  	};

});


function centerInit()
{
        var a = $(".hero-content", "#first_slide"),
                b = ($(window).height() - a.height()) / 3;
        a.css(
        {
                "margin-top": b + "px"
        })
}
 $(document).ready(function()
{
        "use strict";
});